import numpy as np 
# create a 2D NumpY array 
arr1 = np.array([[1, 2, 3], [4, 5, 6]])
arr2 = np.array([[5, 10], [15, 20], [2, 4]])

# now to multiply the 2 D arrays 
array_multiplied = np.dot(arr1, arr2)
print("the matric multiplication of the two 2D arrays are:", array_multiplied)
